# Vinston Voice Assistant - API Keys
# Copy this to .env and fill in your keys

# Anthropic (Claude) - https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-...

# OpenAI (Whisper STT cloud) - https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# STT Provider Configuration
# Options: openai (cloud), mlx-whisper (local Apple Silicon), whisper (local CPU/CUDA)
STT_PROVIDER=openai

# STT Model (for local whisper only)
# MLX options: tiny, medium, large, large-turbo, large-turbo-q4, distil-large
# Faster Whisper options: tiny, base, small, medium, large, large-turbo, distil-medium-en
STT_MODEL=large-turbo

# ElevenLabs (TTS cloud) - https://elevenlabs.io/
ELEVENLABS_API_KEY=...

# TTS Provider Configuration
# Options: elevenlabs (cloud), piper (local)
TTS_PROVIDER=elevenlabs

# TTS Voice (for ElevenLabs: Roger, Adam, Josh, Rachel, Bella, or voice ID)
TTS_VOICE=Roger

# TTS Model Path (for Piper - optional, defaults to ryan-high)
# TTS_MODEL_PATH=/path/to/voice.onnx

# Basic Auth for the voice server
AUTH_USERNAME=vinston
AUTH_PASSWORD=your-secure-password

# Voice server URL (for Clawdbot callback)
# This should be the public URL where the voice server is accessible
VOICE_SERVER_URL=https://voice.ip11.net
